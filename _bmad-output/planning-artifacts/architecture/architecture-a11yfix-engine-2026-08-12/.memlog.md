---
scope: a11yfix-engine
purpose: architecture spine for A11yFix Engine
altitude: initiative
updated: 2026-08-12T21:46
---

- (decision) Paradigm: pipes-and-filters pipeline (harvest->translate->present) with Next.js as orchestration owner; harness=Python, AI+dashboard=TS
- (decision) AD-2: Python service is stateless harvest microservice (POST /scan); never calls AI, never owns data store; boundary prevents two-tier drift
- (decision) AD-3: Audit Report is single versioned envelope (schemaVersion) declared once in shared contracts/ JSON Schema; both tiers validate against it
- (decision) AD-4: Audit Reports immutable; re-scan = new scanId, never in-place mutation
- (decision) AD-5: Analyst and Architect are separate modules over same envelope; Architect emits proposed Patch blocks (status=proposed); nothing auto-applied (product invariant)
- (version) Bound: Playwright ~1.60 (driver), axe-core 4.11.4 (web-verified), FastAPI 0.141.x, next 16.3.0 in-repo, react 19.2.8, tailwind v4, pnpm 11.19
- (decision) AD-6: repo is pnpm workspace monorepo: apps/web (Next.js exists) + services/scanner (Python/FastAPI); contracts/ shared schema at root; dependency direction web->contracts, scanner->contracts, never web<->scanner direct
- (decision) AD-7: AI personas are provider-agnostic via OpenAI-compatible interface; provider + model flagged per persona; free-tier default (OQ-1 binding w/ assumption)
- (decision) CORE-VITALS: measured via Playwright performance API (LCP/INP/CLS) in harness; envelope field vitals{}
- (question) OQ-1 (carried from PRD): which free-tier provider locks in first - deepseek/gemini/groq/ollama? open - not arch-blocking, env-configurable
