---
topic: Story 3.3: Render the Developer View technical detail
updated: 2026-08-17T20:21
---

- (decision) Extract DeveloperView into its own component (developer-view.tsx) mirroring 3.2's client-view.tsx extraction
- (decision) Violation table columns: rule ID (mono), severity chip (text+tint), node count; sortable by all three; default sort severity descending
- (decision) Vitals panel: three metric cards (LCP ms, INP ms, CLS unitless) with null -> 'not measured' SR label; figures use tabular-nums
- (decision) Proposed fixes: consume existing DiffViewer per ArchitectPatch; show rationale + wcag_rule outside dark surface; empty state when no patches
- (decision) data-pending-view-fix consumed: scrollIntoView + focus on matching violation row; the one allowed exception to no-auto-scroll ban
- (constraint) No route, persistence, navigation, or API call; wiring deferral stands; no schema/contract changes; no new dependencies
- (event) Spec created with 6 acceptance criteria, 11 I/O matrix rows, 4 execution tasks; ready-for-dev
